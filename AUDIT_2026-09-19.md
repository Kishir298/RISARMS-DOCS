# RISARMS Full Audit Report — 2026-09-19

> Tracked copy of `RISARMS/AUDIT_REPORT.md` (root is not a git repo, so the canonical file lives at the workspace root; this copy is committed to `RISARMS-DOCS` for history).

Date: 2026-09-19. Scope: all 7 projects, every source file read, pytest run per project (offline + live where feasible).
Auditor: build-mode run with 7 parallel file-read shards. No source fixes applied except env/ignore safety items noted below.

## 0. Root inventory

```
RISARMS/
  ASCS/         A Smart Coding System — Ollama-local coder, coding-agent 0.3.0
  ASIS/         A Smart Intelligence System — assistant/orchestrator, asis 0.1.0
  CORE-CLIENT/  External device client, core-client 0.3.0, stdlib-only
  CORE-HOST/    Windows-11 server/runtime spine (repo remote: Kishir298/CORE), core 0.3.0 + PyYAML
  DOCS/         Architecture source-of-truth (repo: RISARMS-DOCS), markdown-only, 34 docs
  RESCS/        Cloud storage subsystem, rescs 0.3.0, FastAPI + SQLAlchemy
  TIVISS/       Separable AI agent, tiviss 0.1.0, stdlib-only runtime
  Lib/site-packages/pip/ + Scripts/pip3*.exe + .pytest_cache/  — orphan venv fragments at root (no pyvenv.cfg)
```

Each subdir is its own git repo (`main...origin/main`, all clean). Root is NOT a git repo.

## 1. Environment pre-flight (fixed during audit)

- Ollama server UP at `localhost:11434`. Pulled: `qwen3-coder:30b (18GB), qwen2.5-coder:14b, qwen3:14b, qwen2.5:14b`. `ollama ps` was empty (nothing loaded); live tests auto-loaded models (after run: `qwen3-coder:30b` + `qwen2.5-coder:14b` resident on CPU).
- ASCS venv 3.12.14 OK. ASIS venv 3.12.14 OK.
- RESCS + TIVISS venvs BROKEN (base `C:\Python314\python.exe` missing). FIXED by repointing `.venv/pyvenv.cfg` home/executable to the working 3.14.7 base (same version). Verified both launch afterwards. Test-after-edit passed.
- CORE-HOST/CLIENT have no venv; system `py` 3.14.7 has `yaml 6.0.3 + pytest 9.1.1`, used directly.
- CORE-HOST `Lib/site-packages` + `Scripts/windows` only (incomplete in-place venv, no python.exe) — system python used instead.

## 2. Pytest matrix (all run 2026-09-19)

| Project | Command | Result |
|---|---|---|
| TIVISS | `.venv\Scripts\python -m pytest -q` | **155 passed** (0.5s) |
| CORE-CLIENT | `py -m pytest -q` | **78 passed** (2.5s) |
| RESCS | `.venv\Scripts\python -m pytest -q` | **285 passed, 3 skipped** (51s). Skips: `test_postgres_compat` x3 (no `RESCS_INTEGRATION_DATABASE_URL`) |
| CORE-HOST | `py -m pytest -q` | **686 passed, 3 skipped** (42s) |
| ASCS offline | `.venv\Scripts\python -m pytest -q -ra` (`RISALIVE` unset) | **~950 passed, 5 skipped**, exit 0 (955 collected). Skips: 4x `test_ollama_live` + 1x live smoke `test_real_repo_and_toolchain:501` |
| ASCS live | `RISALIVE=1 pytest tests/integration/test_ollama_live.py` | **4 passed**, exit 0 (models auto-loaded, CPU) |
| ASIS offline | `.venv\Scripts\python -m pytest -q -ra` | **~758 passed, 11 skipped**, exit 0 (769 collected). Skips: symlink 1, translation-live 2, voice-real 6, web-live 2 |
| ASIS live web | `ASIS_WEB_LIVE=1 pytest tests/test_web_live.py` | **2 passed** |
| ASIS live voice | `ASIS_REAL_VOICE_TESTS=1 pytest tests/test_voice_real.py` | **1 passed, 5 skipped** (no HW), exit 0 |
| ASIS live translation | `ASIS_TRANSLATION_LIVE=1 pytest tests/test_translation_live.py` | **2 FAILED — expected, model not installed** (`TRANSLATION_MODEL_NOT_INSTALLED`, 0 weight files; needs MADLAD-400 3B MT download) |
| DOCS | n/a | no tests (prose only) |

NOT PERFORMED (no fixture): RESCS live Postgres/S3, CORE physical LAN (needs 2nd host), ASIS mic-dependent voice, MADLAD download.

## 3. Secrets / .gitignore safety

- All repos clean except CORE-HOST `?? tls/` (untracked dir holding secrets + one source file).
- `tls/core.key/.crt/.pem/.pfx` correctly ignored; `config/core.lan.yaml` correctly ignored; only `extract_key.py` inside `tls/` is untracked-source. Do NOT `git add tls/` blindly.
- RESCS `rescs_dev.db`, `rescs_storage/`, `rescs_test_storage/` ignored + untracked. No `.env` files anywhere (only `.env.example`). No tracked secrets in any repo.
- FIX APPLIED + TESTED: `TIVISS/.gitignore` lacked TLS/device patterns → added block; verified with `git check-ignore` (committed to TIVISS `main` as `1b3df11`).
- Root `Lib/`, `Scripts/`, `.pytest_cache/` are stray venv fragments, not in any repo.

## 4. Per-project file audit (every file read)

### 4.1 TIVISS — 49 py, 155 passed.
Bugs: `config.py load()` bool("false")==True; `validate` accepts `provider_id="local"` but `build_provider` rejects; `to_dict()` returns live refs; `rescs.py put/clear` mutate before failure-mode check; `mock.py wordlimit` TypeError escape; `handover.py` non-atomic transitions + no request-membership check. Smells: dead `suppress(MemoryKeyError)`, `reset()` skips hook, mutable dicts in frozen dataclasses, unbounded `events.published`, dev-requirements omits `ruff`.

### 4.2 CORE-CLIENT — 14 py, 78 passed.
`core_device_client.py:940`. Issues: stale session after failed `connect`; `logout` never closes socket / `close_socket` leaves session flags; no `request_id` correlation; `send_to_device` success path always times out (one-way relay); REPL aborts on first app error; `check_hostname=False` always; `load_remembered` TypeError on unknown keys; over-strict `reconnect`; framing errors leave socket reusable. Docs: `session` cmd undocumented; token-handover contradiction.

### 4.3 RESCS — 106 py, 285+3.
`UnprocessableContentError.code` contains comment text; rate-limiter LRU eviction unreachable for new keys + 429 lacks request-ID; uploads `finalize` multi-process CAS overclaimed; `download_stream` skips SHA; S3 selector silent-fakes on missing bucket; SQLA `search` unescaped LIKE; file spool bounds RAM not disk + unsanitized Content-Disposition; multi-owner mode has no read isolation (by design — state explicitly); local-store orphan `.tmp` + fd leak; backup script puts PG URL on cmdline; docs drift (chunk path, stale protocol, `ttl_seconds`).

### 4.4 CORE-HOST — ~133 src py, 686+3.
`config/core.lan.yaml` with real user absolute path is COMMITTED (untrack, keep `.example`); `tls/extract_key.py` stale path + interactive + `NoEncryption` key write + missing dep; validator `agent.auto_assign` double-error + dead flat-key; `ResourceRegistry.discover(category)` None-deref; `InMemory.persist_resource` aliasing; dispatcher `destination="router"`; `execute()` unknown→0; string timestamp compare; `errors["<unknown>"]` collision; fail-open drifts (token→existence fallback, `enforced=False`, localhost plaintext legacy, `loopback` as external, empty `device_type` passes); missing `scripts/windows/install_core_task.ps1`.

### 4.5 ASCS — 89 py, ~950+5 offline, 4 live.
Stale `--num-ctx/--num-predict` help; stale doc paths; count drift (538/5 vs 564/6 vs 7-failed/578); `COMPONENTS.md` line counts stale; `verify_phase0.py` docstring over-promises; dead `_apply_intelligence_defaults`; single-shot loop has no git-dirty guard; `delete_file` allows `.ascs/`; dead tool names in `files_changed()`; planner drops `[...]` JSON + truncates `files[:8]`; millisecond experience-ID collisions; `web.bind()` host string; fragile flat test import; 640-line test duplication.

### 4.6 ASIS — 215 py, ~758+11 offline.
`core/` + `02_voice/` confirmed 0-py stale. God files (engine 970L, assistant 676L, tools 601L, settings/validation/loop 263–368L). Pervasive intentional `except Exception` fail-open (needs logging in bus/lifecycle/formatters). Top-level `import requests` breaks base-install laziness. `os.system("cls")` Windows-only. Empty `LICENSE` + `Proprietary` claim; unpinned voice/dev reqs; venv 3.12 vs `>=3.11`; Windows-only workspace path.

### 4.7 DOCS — 34 md, no tests.
Version matrix triple-consistent; Radar correctly code-free. Drifts: TIVISS remote-only claims in 6 files FALSE (local checkout exists; capability claims hold); ASIS legacy-removal claims in 4 files FALSE (dirs present); `RISARMS-DOCS/` + `RISARMS/CORE/` vs actual `DOCS/` + `CORE-HOST/CLIENT/`; test-count staleness + TIVISS suite omitted; root remnants undocumented.

## 5. Limitations / follow-ups

1. Translation-live red (missing MADLAD weights) — download model or pin `mock` for CI.
2. Postgres/S3 live, physical LAN, mic voice — still NOT PERFORMED (no fixtures).
3. `CORE-HOST config/core.lan.yaml` committed with real path — untrack (`git rm --cached`), keep `.example`.
4. Per-project bugs §4.1–4.6 — fix in priority: CLIENT session hygiene; RESCS CAS + S3-fake + LIKE-escape + stream-SHA; HOST validator + discover-None + lan.yaml + extract_key; ASCS help/paths/planner; TIVISS bool-parse + handover atomicity; ASIS splits + requests laziness; DOCS wording.
5. Root stray `Lib/Scripts/.pytest_cache` — delete or ignore.
6. Pytest prints no `passed` summary line under `-q` here — use `-ra` + exit codes + collect totals; optionally add `junitxml`.
